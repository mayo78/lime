<xml>

	<files id="native-toolkit-dr-libs" tags="" >

		<cache value="1" />
		<tag value="${NATIVE_TOOLKIT_OPTIM_TAG}" if="NATIVE_TOOLKIT_OPTIM_TAG" />

		<compilerflag value="-I${NATIVE_TOOLKIT_PATH}/dr_libs/" />
		<compilerflag value="-I${NATIVE_TOOLKIT_PATH}/custom/dr_libs/" />

		<file name="${NATIVE_TOOLKIT_PATH}/custom/dr_libs/dr_libs.c" />

		<depend name="${NATIVE_TOOLKIT_PATH}/dr_libs/dr_flac.h" />
		<depend name="${NATIVE_TOOLKIT_PATH}/dr_libs/dr_mp3.h" />
		<depend name="${NATIVE_TOOLKIT_PATH}/dr_libs/dr_wav.h" />

	</files>

</xml>